<%= simple_form_for @activity_report do |f| %>
  <%= f.input :activity_instance, collection: ActivityInstance.all %>
  <%= f.input :user_id, value: current_user.id %>
  <%= f.input :hour_estimate %>
  <% if current_user.admin? %>
    <%= f.input :hours_given %>
  <% end %>
  <%= f.input :comentary %>
  <%= f.button :submit %>



<% end %>